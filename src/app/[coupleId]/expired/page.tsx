export default function ExpiredPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-md flex-col items-center justify-center gap-4 px-6 py-12 text-center font-sans">
      <h1 className="font-display text-[28px] tracking-tight text-[var(--color-ink)]">
        link expired
      </h1>
      <p className="text-[15px] leading-relaxed text-[var(--color-ink-soft)]">
        text <span className="font-medium text-[var(--color-ink)]">dashboard</span> to
        cynna in iMessage and we&rsquo;ll send you a fresh link.
      </p>
    </main>
  );
}
